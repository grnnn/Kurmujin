var Color = function(r, g, b) {
  
  this.red = r;
  this.green = g;
  this.blue = b;
  
  //this.red = (parent1.red + parent2.red) / 2;
  //this.green = (parent1.green + parent2. green) / 2;
  //this.blue = (parent1.blue + parent2.blue) / 2;
};

